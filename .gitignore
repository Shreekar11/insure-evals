# Virtual envs
gradio-env/
venv/
.venv/
env/

# Secrets
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# Gradio
flagged/

# HF cache
.cache/
huggingface/

# SQLite — ignore ad-hoc DBs, but keep the committed results DB
*.db
!results/traces.db
# traces.json is the HF-Space-friendly export (text, safe to commit)
# !results/traces.json  ← not needed; .json not globally ignored

# Misc
.DS_Store
*.log
